<?xml version="1.0" encoding="UTF-8"?>
<!--
  switchboard-evaluation - the flag evaluation core, shared by the server and every JVM SDK.

  This module exists so there is exactly ONE Java implementation of flag evaluation. Before
  it, the evaluator lived inside the backend and a Java SDK would have had to reimplement
  MD5 bucketing, sixteen operators, semver ordering and the restricted regex subset - a
  second implementation is precisely the drift that spec/evaluation.md and the conformance
  vectors exist to catch, and catching drift at test time is strictly worse than making it
  impossible.

  The one hard rule: NO dependencies beyond the JDK. Not Spring, not Jackson, not a logging
  facade. An SDK dropped into someone else's application must not drag a dependency tree in
  with it, and this module is the thing both the server and that SDK compile against. The
  conformance test needs Jackson to read the vectors, so it is test-scoped only.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.switchboard</groupId>
    <artifactId>switchboard-evaluation</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>switchboard-evaluation</name>
    <description>Flag evaluation core: bucketing, operators, precedence. JDK-only.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <junit.version>5.14.0</junit.version>
        <jackson.version>2.20.0</jackson.version>
        <checkstyle.version>14.0.0</checkstyle.version>
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
    </properties>

    <dependencies>
        <!-- Test only. Nothing on the compile classpath but the JDK - see the header. -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <!-- Shared with the backend: one style for all Java in the repo. -->
                    <configLocation>../config/checkstyle/checkstyle.xml</configLocation>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                    <failOnViolation>true</failOnViolation>
                    <sourceDirectories>
                        <sourceDirectory>${project.build.sourceDirectory}</sourceDirectory>
                    </sourceDirectories>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle-check</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
            </plugin>
        </plugins>
    </build>
</project>
